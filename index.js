//unction expression divide, divides 2000 by 100
function divide() {
    return (2000 / 100);
}

//pointer function square, multiplies a number by itself
const square = x => x * x;

//pointer function add, accepting two parameters and adds them together
const add = (x, y) => x + y;

console.log(divide());
console.log(square(3));
console.log(add(3,7));